let property = [];
 let continue = true;

 while (continuar) {
    let menu = '\n cadastro de imoveis:\n';
    menu += "quantiade de ${propety.length} imoveis cadastrados\n 1 SALVAR UM NOVO IMOVEL \n 2 LITAR IMOVEIS SALVOS \n 3 SAIR "
 let option = prompt(menu)

if (option === "1"){
    let nomeDoImovel = prompt ("nome do imovel:")
    property.push(nomeDoImovel)
prompt'{$nomeDoImovel'} adicionado ao menu
}}
